# Netlify redirects for Single Page Application
# This ensures all routes go to index.html for client-side routing

/*    /index.html   200

# API routes should be handled by serverless functions (if applicable)
/api/*  /api/:splat  200